local   all             postgres                                trust
hostssl all             testuser        0.0.0.0/0               cert clientcert=verify-full
hostssl all             testuser        ::/0                    cert clientcert=verify-full
host    all             all             0.0.0.0/0               reject
host    all             all             ::/0                    reject
